[project]
name = "estudos-dbt"
version = "0.1.0"
description = "Projeto de aprendizado prático de dbt e Engenharia de Dados com DuckDB"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dbt-duckdb>=1.11.0",
]
